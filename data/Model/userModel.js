import mongoose from "mongoose";

const details = mongoose.Schema({

    username:{
        type: String,
        reqiured: [true, 'username is required']
    }

    username:{
        type: String,
        reqiured: [true, 'username is required']
    }
})